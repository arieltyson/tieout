# Copy to .env and fill in. .env is gitignored and must never be committed.

# iMessage handles permitted to issue commands, comma-separated, E.164.
TIEOUT_ALLOWLIST=+15555550100

# Where the agent sends replies. Usually the same as the allowlist entry.
TIEOUT_REPLY_HANDLE=+15555550100

ANTHROPIC_API_KEY=
